import 'package:flutter/material.dart';

class CadastroAcervo extends StatelessWidget {
  const CadastroAcervo({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('acervo (Em construção)')),
      body: const Center(child: Text('Tela em construção')),
    );
  }
}