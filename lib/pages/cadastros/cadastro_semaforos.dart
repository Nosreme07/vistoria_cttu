import 'package:flutter/material.dart';

class CadastroSemaforos extends StatelessWidget {
  const CadastroSemaforos({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Semáforos (Em construção)')),
      body: const Center(child: Text('Tela em construção')),
    );
  }
}