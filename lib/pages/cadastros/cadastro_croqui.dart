import 'package:flutter/material.dart';

class CadastroCroqui extends StatelessWidget {
  const CadastroCroqui({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('croqui (Em construção)')),
      body: const Center(child: Text('Tela em construção')),
    );
  }
}