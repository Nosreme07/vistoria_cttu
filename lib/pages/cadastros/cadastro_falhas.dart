import 'package:flutter/material.dart';

class CadastroFalhas extends StatelessWidget {
  const CadastroFalhas({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Falhas (Em construção)')),
      body: const Center(child: Text('Tela em construção')),
    );
  }
}